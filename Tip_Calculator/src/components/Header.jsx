import logo from '../assets/logo.svg';

const Header = () => {
    return (
        <div>
            <h1>
                <img src={logo} alt="logo splitter" />
            </h1>
        </div>
    )
}

export default Header;